{
  "name": "gitops",
  "version": "1.2.3",
  "description": "Projeto GitOps com deploy automático",
  "main": "index.html",
  "scripts": {
    "start": "python -m http.server 3000",
    "build": "mkdir -p dist && cp -r src/* dist/",
    "test": "echo 'No tests specified' && exit 0",
    "lint": "echo 'No linting configured'",
    "dev": "docker-compose up --build",
    "deploy": "kubectl apply -f k8s/",
    "k8s-status": "kubectl get all -n gitops"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rickOsantos/gitops.git"
  },
  "keywords": [
    "gitops",
    "kubernetes",
    "ci-cd",
    "devops",
    "docker"
  ],
  "author": "Ricardo Santos",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/rickOsantos/gitops/issues"
  },
  "homepage": "https://github.com/rickOsantos/gitops#readme",
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}